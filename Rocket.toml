[default.databases.manga_server]
# Mysql
# make sure to enable "sqlx-mysql" feature in Cargo.toml, i.e default = ["sqlx-mysql"]
url = "mysql://admin:admin123@localhost/manga_server"
timeout = 100

[default.tls]
certs = "orange.crt"
key = "orange.key"

[default]
address = "0.0.0.0"

[release]
port = 9432